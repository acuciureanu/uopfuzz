# Security-focused npm configuration
# Prevents postinstall scripts from running automatically
ignore-scripts=true

# Use audit level for security checks
audit-level=moderate

# Always save exact versions
save-exact=true

# Don't automatically update packages
save-prefix=''
